 git --version
 git status
 Go to the folder cd
 git init
 git config --global user.name "UserName"
 git config --global user.email "User Email" 
 git remote add origin "Remote repository url copied from Github"
 git add -A
 git commit -m "Commit message"
 git push -u origin main
 git branch
 git branch branch name
 git checkout branch name
 git merge branch name /Checkout main branch and perform the branch merge 
 git branch -d branch Name
 git pull origin "Remote repository url copied from Github"
 git clone "Remote repository url copied from Github"
 
 git restore //to restore the files from Staging Area to Working Space
 git touch .gitignore //Creates a file to update the ignorable files(Say, *.log, ) add to Staging, commit to local and push to  remote repos